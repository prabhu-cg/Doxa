// Shared, cross-feature type definitions go here. Types local to one
// feature belong in that feature's own `types.ts` instead (see
// `src/features/README.md`).
//
// Empty in Phase 0 — the first shared types (Organisation, Membership,
// Role) land in Phase 1.

export {};
